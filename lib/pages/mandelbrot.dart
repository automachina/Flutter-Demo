import 'package:flutter/material.dart';
import 'package:test_drive/components/mandelbrot/mandelbrot.dart';

class MandelbrotPage extends StatelessWidget {
  const MandelbrotPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Mandelbrot();
  }
}
